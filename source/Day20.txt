在基於 UiPath 的付款對帳研究中，`generate_ar.py` 腳本的主要功能是什麼？	建立包含 1,000 名客戶的合成 ERP 匯出資料，其中包括發票、付款、信用和調整記錄。
在合成的 Accounts Receivable 資料集中，大約有多少比例的行被刻意設置為「損壞」以供系統檢測？	10% 至 12%。
請寫出 `reconcile.py` 用於重新計算客戶餘額的公式。	$Balance = ((Invoice.total - Invoice.applied) \cdot exchange\_rate) - ((Payment.total - Payment.applied) \cdot exchange\_rate) - ((Credit.total - Credit.applied) \cdot exchange\_rate) + ((Adjustment.total - Adjustment.applied) \cdot exchange\_rate)$
在對帳自動化項目中，`anomalies.py` 腳本使用哪種機器學習模型來預測餘額並識別異常？	線性迴歸 (Linear Regression)。
UiPath 工作流分析器 (Workflow Analyzer) 的「文件發現」步驟具體做什麼？	遞迴搜尋專案目錄中所有的 .xaml 文件，同時忽略臨時和版本控制目錄。
UiPath 工作流分析器如何構建「依賴圖」？	建立一個有向圖，將每個工作流與其調用的子工作流集合關聯起來。
在 UiRPA 實施後，發票處理時間從每張 7 分鐘減少到了多少？	1.5 分鐘 (減少了 78.57%)。
實施 UiRPA 後，訂單輸入準備時間的改善百分比是多少？	65%。
根據研究結果，自動化後的庫存付款對帳週期從 6 天縮短到了幾天？	2 天。
UiRPA 實施後，發票匹配錯誤率從 15% 顯著降低到了多少？	1.50% (降低了 90%)。
自動化對帳系統預期可節省多少比例的營運成本？	20% 至 25%。
什麼是「自動化銀行對帳」？	使用軟體自動匹配銀行對帳單與總帳交易，標記未匹配項並生成審計報告，無需手動逐行比對。
在 Python 對帳腳本中，為什麼在進行精確匹配前需要對資料進行「標準化」？	為了統一格式（如轉為小寫、移除空格），確保不同來源的欄位名稱能正確對齊。
Python 自動化對帳的第二步通常是什麼？	根據「金額」和「日期」進行精確匹配 (Exact Match)。
在銀行對帳中，為什麼需要使用「模糊匹配」邏輯？	為了處理結算延遲（Timing differences），例如月底的入帳可能在次月初才出現在對帳單。
Python `fuzzy_match` 函數中常用的兩個容差參數是什麼？	日期容差 (date_tol) 和金額容差 (amt_tol)。
對於使用 Microsoft 365 的團隊，如何定期調度 Python 對帳腳本並通知異常？	透過 Power Automate 雲端流觸發 Azure Function 執行腳本，並將異常發送到 Teams 頻道。
開發一個基本的 Python 銀行對帳腳本通常需要多少天？	2 至 3 天。
Quadratic 銀行對帳模板中，預設的「日期容差窗口」是多少？	$\pm 1$ 天。
Quadratic 模板中的「描述匹配閾值」預設設定為多少百分比？	50%。
在 Quadratic 儀表板中，「未匹配項帳齡分析 (Unmatched By Age)」圖表的作用是什麼？	針對仍未對帳的交易進行帳齡分析，協助用戶優先處理較舊的項目。
實體匹配 (Entity Matching) 在應收帳款結算中的主要目標是什麼？	將銀行轉帳資料（通常是非結構化文字）與正確的組織成員或債務人匹配。
在非政府組織 (NGO) 的自動對帳案例中，銀行轉帳資料中僅有多少比例包含正確的 ID？	約 65%。
實體匹配中「得分後處理 (Score post-processing)」的基本思想是什麼？	將特定交易的所有候選匹配得分重新縮放，使其總和為 1，以觀察「相對」得分差距。
串聯模型 (Chain classifier) 在實體匹配中的迭代次數上限通常是多少？	3 次迭代。
在波蘭語環境下的實體匹配，如何處理姓名和姓氏的語法變化？	檢查姓名和姓氏的所有語法形式 (Declination)，並使用 Levenshtein 距離進行軟匹配。
根據研究，實體匹配方法將精確率 99% 時的召回率 (Recall) 從 78% 提升到了多少？	90% 以上。
哪三種核心條件使一項任務非常適合自動化？	1. 格式固定、2. 規則明確、3. 每月重複。
在使用 AI 輔助撰寫 PowerPoint VBA 時，為什麼建議上傳簡報截圖？	讓 AI 了解標題是文字方塊、圖形群組還是母片內容，以寫出精準的程式碼。
若要保存包含 VBA 聚集的 PowerPoint 檔案，必須選擇哪種副檔名？	.pptm。
在合併 Excel 檔案時，`Worksheet.Copy` 與 `UsedRange.Copy` 有何主要區別？	`Worksheet.Copy` 複製整張工作表物件（保留名稱與格式），而 `UsedRange.Copy` 僅複製資料區塊。
在 VBA 中，`ThisWorkbook.Path` 屬性的作用是什麼？	動態取得目前活頁簿所在的資料夾位置。
VBA 中的 `Dir` 函數在對帳流程中主要用於什麼目的？	在執行後續動作前，檢查來源檔案是否存在（防呆機制）。
在 VBA 中使用 `SaveChanges:=False` 關閉來源檔案的目的是什麼？	關閉檔案且不儲存任何變更，確保原始檔案不被干擾或覆蓋。
若要保存包含 VBA 聚集的 Excel 檔案，必須選擇哪種副檔名？	.xlsm。
RowSpeak 等 AI 工具與傳統 Excel 工作流程相比，對帳時間有何差異？	傳統 Excel 需 1-3 小時，RowSpeak 僅需 1-2 分鐘。
根據恆遠數位科技，2026 年「發票辨識 OCR」場景的技術成熟度為何？	極高。
在財務自動化中，「財報分析與異常偵測」場景的導入難度被評為？	中高。
Gartner 預測到 2029 年，策略性部署 AI 的 CFO 將為公司增加多少利潤成長？	10 個百分點。
與舊系統相比，2026 年結合 LLM 的 OCR 系統在辨識品名時有何進步？	能理解上下文，不再死板地照欄位讀取（例如能區分型號與數量）。
判斷類工作（如預測、分析、決策）與流程自動化相比，哪一類的 AI 效益更高？	判斷類工作的 AI 效益遠高於單純的流程自動化。
在對帳流程中，標準化發票編號通常包括哪些操作？	移除額外空格、更換大小寫、移除特殊字元以及修正無效的分隔符號。
UiPath 的「智慧文件處理 (IDP)」層通常結合了哪些技術？	OCR、機器學習 (ML)、自然語言處理 (NLP) 和 RPA。
在 UiPath 專案中，Orchestrator 佇列 (Queues) 用於什麼？	任務分配、狀態追蹤及實現工作負載的水平擴展。
自動化對帳系統中的「商業異常」與「系統異常」有何不同？	商業異常源於資料邏輯（如金額不符），系統異常源於技術問題（如伺服器超時）。
為什麼在大型財務自動化專案中推薦使用 REFramework？	因為它提供了標準化的錯誤處理、重試機制和狀態管理架構。
在 Python `pandas` 中，`str.strip().str.lower().str.replace(" ", "_")` 的目的是什麼？	清洗欄位名稱，移除首尾空格、轉小寫並將空格替換為底線以便於操作。
Python 對帳報告中，通常會使用哪種庫來設定 Excel 儲存格的顏色（如標記異常為紅色）？	`openpyxl` (使用 `PatternFill`)。
Quadratic 模板如何計算「匹配信心分數」？	根據記錄與配置參數（描述相似度、日期接近度、金額差異）的貼合程度進行評分。
實體匹配中的「級聯模型 (Cascade model)」第一階段輸出是什麼？	初步的匹配得分向量和排名第一的候選匹配項。
在應收帳款自動結算中，「精確率 (Precision) >= 99%」的重要性為何？	修正錯誤匹配所需的成本遠高於手動處理缺失匹配，因此必須確保極高的準確度。
為什麼自動化系統需要對「匯率」和「四捨五入誤差」設置容差規則？	為了避免因微小的匯算差異或數值截斷導致對帳失敗。
在 PPT VBA 自動化中，如何處理從 12 月到下一年 1 月的過渡？	在程式碼中編寫年度進位邏輯，確保 12 月後更新為次年 1 月，而非「13 月」。
根據中小企業老闆的經驗，會計部門約有多少比例的工作是「純粹的搬運工作」？	約 60%。
會計部門導入 AI 的一個關鍵跡象是：當老闆詢問利潤時需等待多久？	若需等待 3 天以上才有答案，即為導入時機。
在「發票辨識」SOP 中，AI 能夠辨識台灣的哪些憑證類型？	三聯式、二聯式、收銀機發票、電子發票，甚至手寫補開憑證。
自動化對帳中使用的「重複行檢索欄位」通常包括哪些？	發票編號、付款金額、付款日期、銀行參考號和付款人標識符。
在 `Algorithm 1` 中，OCR 引擎提取的文本 $T$ 會發送給什麼來生成結構化 JSON？	大型語言模型 (LLM) $L$。
UiPath 工作流分析器的「執行順序確定」步驟使用哪種演算法遍歷依賴圖？	深度優先搜尋 (DFS)。
實體匹配研究中使用的「XGBoost」模型在處理類別失衡時有何優勢？	它是一個強大的整合模型，只需調整偏置或截止值即可處理正負樣本比例失調。
什麼是「DSO (Days Sales Outstanding)」指標？	應收帳款週轉天數，即從銷售完成到收回款項所需的平均天數。
在 VBA 合併檔案流程中，「人工驗證」應檢查哪些重點？	檢查工作表是否新增、月份欄位是否正確、抽查總比數以及比對內容一致性。
對於每月處理超過多少張發票的 1-5 人會計部門，2026 年是導入 AI 的好時機？	每月超過 200 張發票。
「記帳分錄自動化」場景預計每月可節省多少工時？	20 至 40 小時。
在 UiPath 工作流分析中，如何區分不同的工作流角色？	若專案被識別為 REFramework 實施，分析器會根據框架角色對工作流進行分類。
Python 對帳腳本在進行模糊匹配時，若找到多個候選匹配項會如何處理？	範例程式碼中通常設定為「僅當候選項只有一個時」才執行自動匹配，以確保準確。
RowSpeak 工具如何處理缺失交易（Missing transactions）？	自動標記並生成建議，例如「交易 ID 在系統 A 存在但在系統 B 缺失」。
「實體去重 (Entity deduplication)」是實體匹配技術的另一種應用場景嗎？	是的。
在 VBA 中，若要在同一活頁簿中新增多個名稱相同的工作表，Excel 會如何處理？	會自動重新命名（例如加上編號 2），以防止名稱衝突。
什麼是「Cash Application」自動化？	將收到的付款自動匹配到未清償發票，並在 ERP 系統中進行結算的過程。
自動化對帳中的「通融限額 (Tolerance limits)」通常應用於什麼情況？	處理因四捨五入或微小差額導致的金額不一致。
在 UiPath 專案中，「資產 (Assets)」主要用於存儲什麼？	環境特定的配置、憑據或其他共享變量。
機器學習異常檢測在對帳中的作用是什麼？	發現隱含的整合錯誤，即使資料通過了基於規則的檢查，仍能識別出不符合整體模式的記錄。
為什麼自動化對帳系統需要生成「審計追蹤 (Audit trail)」？	為了符合財務合規要求，確保每個對帳決定和異常處理都有完整的記錄可查。
在 VBA 中執行聚集後，為什麼建議彈出訊息框 (MsgBox)？	向使用者確認程式已執行完畢，並提供成功或失敗的即時回饋。
Python 對帳自動化的成本結構通常包括哪些部分？	通常包括開發與測試（2-5天）以及後續的雲端執行或託管費用。
在 entity matching 中，什麼是「特徵向量 (Features vector)」？	由兩個記錄屬性之間的相似度計算出的數值集合（如姓名、地址是否匹配）。
根據 2026 年的報告，多少比例的企業表示財務 AI 的投資回報率 (ROI) 達到或超過預期？	71%。
UiPath 研究顯示，自動化能減少多少比例的發票匹配錯誤？	90%。
「吞吐量 (Throughput)」在對帳系統性能評估中代表什麼？	系統在單位時間內能處理的紀錄或動作總量（例如 40,000 筆）。
